language: python
cache: pip
python:
  - '2.7'
env:
  - TOXENV=pep8
addons:
  apt:
    packages:
before_install:
  - pip install -r requirements.txt
  - pip install -r requirements_test.txt
install:
  - tox
script:
  - py.test --cov=./
after_success:
  - bash <(curl -s https://codecov.io/bash)
